{
  "name": "wulv-test1",
  "engines": {
    "node": ">=8.11.0"
  },
  "version": "0.1.1",
  "dependencies": {
    "wulv-utils-cli": "^0.0.3"
  },
  "scripts": {
    "dev": "wulv-utils-cli dev",
    "build": "wulv-utils-cli build",
    "test": "wulv-utils-cli test",
    "publish": "wulv-utils-cli publish"
  },
  "devDependencies": {
    "coveralls": "^3.0.2"
  }
}
